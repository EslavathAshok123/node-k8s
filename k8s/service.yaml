apiVersion: v1
kind: Service
metadata:
  name: node-k8s-local-service:${BUILD_NUMBER}
spec:
  type: NodePort
  selector:
    app: node-k8s-local
  ports:
  - port: 8001
    targetPort: 8000
    nodePort: 32000
